{
  "name": "troveofgems_archive",
  "version": "1.0.0",
  "description": "Trove of Gems Archive and Knowledge Base",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "concurrently \"cd ./febe/client && npm run start\"",
    "server": "concurrently \"cd ./febe/server && npm run start\"",
    "project": "concurrently \"npm run server && npm run client\""
  },
  "private": true,
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
